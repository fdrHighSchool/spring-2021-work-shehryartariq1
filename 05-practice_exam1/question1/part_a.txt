private string recombine(string word1, string word2){


 string com = word1.substring(0,word1.length/2);
 string ined = word2.substring(word2.length/2);

string combined = com + ined;
return combined
